import React from 'react';

import './PageSpinner.scss';

const PageSpinner = () => (
  <div className="page-spinner">
    <span />
    <span />
    <span />
    <span />
  </div>
);

export default PageSpinner;
